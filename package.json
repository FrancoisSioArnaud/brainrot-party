{
  "name": "brainrot-party",
  "private": true,
  "scripts": {
    "build": "cd contracts && npm run build && cd ../backend && npm run build && cd ../frontend && npm run build",
    "typecheck": "cd contracts && npm run typecheck && cd ../backend && npm run typecheck && cd ../frontend && npm run typecheck"
  }
}
